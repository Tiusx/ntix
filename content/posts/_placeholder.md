# placeholder

This file exists only to keep the webpack dynamic-import context for
`@content/posts/*.md` resolvable when the posts directory is empty.
It has no frontmatter, so it is filtered out and never rendered.